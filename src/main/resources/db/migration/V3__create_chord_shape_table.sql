CREATE TABLE chord_shape (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL UNIQUE,
    instrument VARCHAR(255) NOT NULL DEFAULT 'guitar',
    frets VARCHAR(255) NOT NULL,
    fingers VARCHAR(255)
);